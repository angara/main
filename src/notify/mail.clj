
(ns notify.mail
  (:require
    [clojure.string :as s]))
;
;
; (into {} (map #(vector % (int %))) "Proto REPL")
;
;
; (defn myfunc "my function"
;   [])
; ;
;
;
; (go)
;
; clojure.string/join
;
;
; (let [numbers (range 1 12)]
;   (map #(* 2 %) numbers))
;
;;.
